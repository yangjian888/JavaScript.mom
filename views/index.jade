extends layout

block content
  h1#logo.container=title
  .container.typo
    h4 文章归档
    section#posts
      each posts, index in body
        div
          i=(index + 1) + ' . '
          a(href=`./article/${posts.number}`)=posts.title
          small= ' ' + posts.created_at
          p='no description'
  script(src='./javascripts/bundle.js')
